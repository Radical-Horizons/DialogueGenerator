/**
 * Infobulle native (`title`) pour le déplacement des nœuds dans le graphe.
 */
export const NODE_DRAG_TOOLTIP =
  'Glisser : ce nœud et toutes ses suites se déplacent ensemble. Ctrl+glisser : ce nœud seul.'

/**
 * Infobulle pour les TestNodes (4 handles) : le drag ne déplace pas les résultats liés.
 */
export const TEST_NODE_DRAG_TOOLTIP =
  'Glisser : déplace uniquement ce nœud de test (les résultats liés restent en place).'
