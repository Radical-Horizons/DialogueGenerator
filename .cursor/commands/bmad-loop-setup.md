---
name: bmad-loop-setup
description: 'Install or upgrade BMad Loop (orchestrator + project hooks). Use for first-time setup or upgrade. Example: accept all defaults.'
---

IT IS CRITICAL THAT YOU FOLLOW THIS COMMAND:

1. LOAD the FULL skill file from {project-root}/.agents/skills/bmad-loop-setup/SKILL.md
2. READ its entire contents and follow its directions exactly
3. If the user passed arguments (e.g. `accept all defaults`, `upgrade`), apply them per the skill's headless/upgrade rules

Prerequisites (from BMad Loop docs): Python, tmux, uv, and a supported coding CLI. On native Windows, BMad Loop expects WSL.
