# Team overrides for bmad-retrospective (DialogueGenerator)
# Merge layer: _bmad/custom/bmad-retrospective.toml → skill customize.toml

[workflow]

persistent_facts = [
  "file:{project-root}/_bmad/custom/retrospective-dialoguegenerator.md",
  "All retrospectives for this repo require Step 1.5 manual UI validation before continuing (never skip, even in YOLO).",
  "Before saving the retro document, align semver per .cursor/rules/app_versioning.mdc and prod-release skill.",
]
